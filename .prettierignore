# directories
build/
node_modules/
public/
.worktrees/
.github/
apps/research/.next/
apps/research/.wrangler/
apps/research/.sites-runtime/
apps/research/dist/
apps/research/node_modules/

# lockfiles
bun.lock
package-lock.json

# content we never want reflowed
src/_data/guestbook.json
src/now/updates/
src/writings/
src/library/
src/odysseys/
src/projects/

# auto-generated
assets/
apps/research/data/catalogs/

# has JSON-LD inside {% if %} branches, breaks the jinja parser
src/_includes/layouts/base.njk

# has Nunjucks {{ }} inside <script> tags, breaks the jinja parser
src/guestbook.njk

# XML feed + sitemap — prettier adds a blank line between the frontmatter
# fence and <?xml?>, which breaks XML parsers (declaration must be byte 0).
src/feed.njk
src/sitemap.njk

# Plain-text Nunjucks; HTML formatting destroys the Markdown line structure
src/llms.txt.njk

# Emits raw JSON rather than HTML; the Jinja formatter cannot parse it
apps/research/site/index.njk

# Nunjucks control flow with conditional </ol> inside {% if %} confuses the jinja parser
src/sitemap.html.njk
