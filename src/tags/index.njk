---
title: "All Tags"
layout: layouts/base
permalink: /tags/
---
<section class="max-w-5xl mx-auto px-6 py-20 space-y-8">
  <h1 class="text-4xl md:text-5xl font-serif font-bold mb-10 text-center">All Tags</h1>
  <div class="grid grid-cols-2 pl-2 pr-2 md:grid-cols-3 lg:grid-cols-4 gap-y-3">
    {% for tag, posts in collections %}
      {% if not (["all", "nav", "post", "posts", "tagList", "library", "books", "writings", "papers", "paper", "projects"].includes(tag)) %}
        <a href="/tags/{{ tag | slugify }}/" 
           class="font-semibold hover:underline decoration-2 underline-offset-4">
          {{ tag }} <span class="text-muted">({{ posts.length }})</span>
        </a>
      {% endif %}
    {% endfor %}
  </div>
</section>

