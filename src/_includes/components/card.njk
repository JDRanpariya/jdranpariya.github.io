{% macro card(title, description, link) %}
<a href="{{ link }}" class="block bg-white rounded-2xl shadow-md p-6 transition hover:shadow-lg hover:scale-105 transform">
  <h3 class="text-xl font-playfair mb-2">{{ title }}</h3>
  <p class="text-gray-700">{{ description }}</p>
</a>
{% endmacro %}

