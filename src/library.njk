---
title: Library
layout: layouts/base
---

<section class="max-w-6xl mx-auto px-6 py-24 md:py-32 flex flex-col gap-16">

  <!-- Header -->
  <div class="text-center space-y-4">
    <h1 class="text-5xl md:text-6xl font-serif text-accent">Library</h1>
    <p class="text-lg md:text-xl text-secondary max-w-3xl mx-auto">
      A curated shelf of books, papers, and resources that inspire me or shape my thinking.
      Browse through my favorites and recommended readings.
    </p>
  </div>

  <!-- Book Shelf -->
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
    {% for book in collections.library %}
    <div class="bg-card rounded-xl shadow-md p-4 hover:shadow-lg transition-shadow flex flex-col items-center">
      <img src="{{ book.data.cover }}" alt="{{ book.data.title }}" class="rounded-lg mb-4 w-full h-64 object-cover">
      <h3 class="text-xl font-serif text-accent text-center mb-1">{{ book.data.title }}</h3>
      <p class="text-secondary text-center mb-2">{{ book.data.author }}</p>
      <a href="{{ book.data.link }}" target="_blank" class="link text-accent hover:underline">More info →</a>
    </div>
    {% endfor %}
  </div>

</section>

