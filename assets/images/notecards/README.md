# Notecard textures

See [docs/guestbook.md](../../docs/guestbook.md) for the full management
guide — adding, removing, theming, and ink color rules.

## Rules for this folder

- Only `notecard-<slug>.webp` and this `README.md` allowed here.
- The smoke test fails if stray files (JPG, PNG, etc.) are present.
- Keep raw sources in `scratch/notecard-sources/` (gitignored).
